A stat tracker for different modes in Kovaak's

Plan:
# Input phase
    # During input have user put in training type or select from already made input types
    # Have user put in scores
    # automatically put in current data
    
# Output phase
    # Ask the user if they want to see a full graph of the whole history of the specific training
    # Show the graph for the specific training type
    # Have the graph show Lowest and highest in a day w/ dates
    # Highlight in between the two graphs